server {
    listen 80;
    server_name api.imphien.xyz;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name api.imphien.xyz;
    root /var/www/html/public;

    ssl_certificate /etc/letsencrypt/live/api.imphien.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.imphien.xyz/privkey.pem;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
        autoindex off;
        index index.php;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    location ~ \.php$ {
        set $path_info $fastcgi_path_info;
        fastcgi_index index.php;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include /etc/nginx/fastcgi_params;
        fastcgi_pass $PHP_FPM_SERVER:$PHP_FPM_PORT;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param PHP_VALUE "upload_max_filesize=50M \n post_max_size=50M";
    }
}

server {
    listen 80;
    server_name cms.imphien.xyz;

    location / {
        proxy_pass http://221.132.33.175:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
